import { Todo } from '../Todo/Todo';
import './Todos.css'

export const Todos = ({todos}) => {
    return (
        <div className="todos-container">
            {/* 
                Map through the todos array (from the props)
                and render a Todo component for each todo 
                (see the Todo component for more info)
            */}
            
        </div>
    )
};